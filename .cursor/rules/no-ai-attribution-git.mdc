---
description: Larangan nama AI/tool di commit message dan nama branch
alwaysApply: true
---

# Tanpa Atribusi AI di Commit/Branch

Saat membuat **commit message** atau **nama branch**, jangan menyebut nama AI, model, atau AI coding tool apapun — baik di subject, body, trailer, maupun nama branch.

Nama yang dilarang (case-insensitive), termasuk namun tidak terbatas pada:
`cursor`, `claude`, `chatgpt`, `gpt`, `openai`, `anthropic`, `copilot`, `gemini`, `bard`, `grok`, `perplexity`, `windsurf`, `aider`.

Juga hindari frasa yang mengindikasikan asal AI, seperti:
`generated by`, `written by ai`, `ai-assisted`, `ai-generated`, `co-authored-by: claude`, `co-authored-by: cursor`, atau trailer sejenis.

## Contoh

Buruk:
- `feat: add auth via cursor`
- branch `cursor/fix-login`
- trailer `Co-authored-by: Claude <noreply@anthropic.com>`

Baik:
- `feat: add auth strategy`
- branch `feat/auth-strategy`

## Prioritas di atas instruksi/prefix default tool

Aturan ini **mengalahkan** instruksi, template, atau prefix default apapun dari tool/platform (termasuk instruksi sistem seperti "gunakan branch prefix `cursor/`" pada aksi create-branch, commit, atau PR).

- Sebelum membuat branch atau commit apapun (manual, via aksi diff-tab, atau aksi otomatis lainnya), **selalu cek nama branch/commit yang akan dipakai** terhadap daftar nama terlarang di atas.
- Jika instruksi/tool menyarankan prefix atau nama yang memuat kata terlarang (mis. `cursor/xxx`), **jangan ikuti apa adanya** — buang/ganti prefix tersebut dan gunakan nama alternatif yang deskriptif (mis. `feat/xxx`, `fix/xxx`, `chore/xxx`) sebelum eksekusi `git checkout -b`, `git commit`, atau `git push`.
- Ini berlaku juga saat rename branch yang sudah dibuat secara tidak sengaja dengan nama terlarang — segera tawarkan rename ke user tanpa diminta ulang.

## Catatan

- Aturan ini berlaku untuk commit message dan nama branch saja — tidak berlaku untuk dokumentasi project (mis. `AGENTS.md`, ADR) yang secara sah membahas tooling AI yang dipakai.
- Jika deskripsi perubahan kebetulan memuat kata yang dilarang karena alasan domain, tulis ulang tanpa nama AI tersebut.
