/**
 * T-032.6 — smoke re-export. Impor hanya dari `motion/react` (bukan
 * `framer-motion`). Rewrite `home-motion.tsx` = T-036.
 */
export { AnimatePresence, motion, useReducedMotion } from "motion/react";
